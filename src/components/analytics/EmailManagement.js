import React, {Component} from "react";

export default class EmailManagement extends Component {
  state = {
  };

  render() {
    return (
      <div className="container">Email Management</div>
    )
  }
}